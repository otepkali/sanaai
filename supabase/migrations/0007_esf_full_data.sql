-- Полная структура ЭСФ (все разделы A-L) гораздо шире набора колонок, заданного
-- в 0006 — храним весь объект целиком в jsonb, отдельные колонки оставляем для
-- быстрых выборок в истории (номер, покупатель, суммы, статус).
alter table esf_documents
  add column if not exists data jsonb;
